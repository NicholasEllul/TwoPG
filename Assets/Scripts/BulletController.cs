using UnityEngine;
using System.Collections;

public class BulletController : MonoBehaviour {
    /*
    public float bulletSpeed;

    // Use this for initialization
    void Start () {
	
	}

	void OnBecameInvisible()
    {
        
    }

	// Update is called once per frame
	void Update () {
        transform.Translate(0, bulletSpeed, 0);
    }

    public void Shoot()
    {

        if (myId == ID.Red && RedAmmo > 0)
        {
            RedAmmo = RedAmmo - 1;
            shot = true;
            Instantiate(bulletPrefab, myObj.transform.position, myRotation);
            //    transform.position = myObj.transform.position;
        }
        else if (myId == ID.Blue && BlueAmmo > 0)
        {
            BlueAmmo--;
            shot = true;
            Instantiate(bulletPrefab, myObj.transform.position, myRotation);
            //   transform.position = myObj.transform.position;
        }
        Debug.Log(RedAmmo + " Red");
        Debug.Log(BlueAmmo + " Blue");
        CheckAmmo();

    }*/
}
